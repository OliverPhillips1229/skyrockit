<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Application Details</title>
  </head>
  <body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Application Details</h1>
  
  <h2><%= application.title %> at <%= application.company %></h2>
  
  <p><strong>Status:</strong> <%= application.status %></p>
  
  <% if (application.notes) { %>
    <p><strong>Notes:</strong></p>
    <p><%= application.notes %></p>
  <% } %>
  
  <% if (application.postingLink) { %>
    <p><strong>Job Posting:</strong> <a href="<%= application.postingLink %>" target="_blank">View Job Posting</a></p>
  <% } %>
  
  <a href="/users/<%=user._id%>/applications/<%= application._id %>/edit">Edit Application</a>
  <a href="/users/<%=user._id%>/applications">Back to All Applications</a>
  
  <form action="/users/<%=user._id%>/applications/<%= application._id %>?_method=DELETE" method="POST" style="margin-top: 20px;">
    <button type="submit" onclick="return confirm('Are you sure you want to delete this application?')" style="background-color: red; color: white;">Delete Application</button>
  </form>
</body>
</html>
